// shortcut if else with ternary operator
const money = 1000;
let food = money > 100 ? 'biryani' : 'maser bortha';
// console.log(food);

const active = false;

const cssClass = active ? 'active' : 'inactive';
// console.log(cssClass);

const x = active && 5;
const y = active || 5;
// console.log(y);

// number to string
const number = +'33';
console.log(number);

// string to number
const string = 33 + '';
console.log(string);

